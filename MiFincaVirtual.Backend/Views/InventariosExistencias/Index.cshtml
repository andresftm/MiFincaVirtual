@model IEnumerable<MiFincaVirtual.Common.Models.InventariosExistencias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Opciones.Codigopcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GramosConsumoDiaLote)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorUnitarioInventario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Opciones.Codigopcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GramosConsumoDiaLote)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorUnitarioInventario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InventarioExistenciaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InventarioExistenciaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InventarioExistenciaId })
        </td>
    </tr>
}

</table>
